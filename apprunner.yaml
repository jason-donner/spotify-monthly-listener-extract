version: 1.0
runtime: docker
build:
  commands:
    build:
      - echo "Building Spotify Monthly Listener Extract for AWS App Runner..."
run:
  network:
    port: 8080
    env:
      - AWS_REGION=us-east-1
      - PORT=8080
      - FLASK_DEBUG=false
      - LOG_LEVEL=INFO
      - LOG_TO_STDOUT=true
